# Wildcard DNS for any name under .local -> loopback
# Install with dnsmasq (see scripts/setup-wildcard-local-dns.sh).
#
# Note: .local is also used by mDNS (Avahi). If you see odd resolution,
# prefer another TLD for dev (e.g. .test) or disable Avahi for .local.

address=/.local/127.0.0.1
